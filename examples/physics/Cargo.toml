[package]
name = "physics"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
glucore_core = { path = "../../core" }
glucore_macro = { path = "../../adapters/rust" }
inventory = "0.3"
